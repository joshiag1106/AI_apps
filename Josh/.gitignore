node_modules/
dist/
*.log
npm-debug.log*

# OS cruft
.DS_Store
Thumbs.db

# Generated by `npm run icon`
build/icon.png

# Downloaded by `predist:win` (see scripts/fetch-win-tools.js). Pinned by URL
# and SHA256 in that script rather than committed, so the binary is verifiable
# instead of an opaque blob in git history.
vendor/
