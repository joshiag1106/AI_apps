<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Entitlements for the macOS hardened runtime.

      V8 compiles JavaScript at runtime, so it needs JIT and writable
      executable memory. The native PTY module is a separately signed .node
      binary, which requires library validation to be relaxed. Without these
      three, a signed and notarised build crashes on launch.
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!--
      A terminal launches arbitrary user programs, which may in turn read the
      user's files. Inheriting these keeps child processes working the way
      they do in Terminal.app.
    -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.inherit</key>
    <true/>
</dict>
</plist>
